# El Dockerfile corre en Linux y su CMD lanza comandos de shell; con CRLF esos
# comandos se corromperían. Fuerza LF sin importar el core.autocrlf del clon.
Dockerfile      text eol=lf
*.sh            text eol=lf
